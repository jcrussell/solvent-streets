# Austin, TX — single-city example
#
# Austin has excellent open data coverage. This example uses only
# OpenStreetMap via Overpass with default settings.

[forecast]
growth_rate = 0.015

[[cities]]
name = "Austin, TX"
overpass = true
