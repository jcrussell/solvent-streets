// Type-checks the index page script in isolation (see tsconfig.json for why the
// two page scripts are checked as separate Programs). Run via `npm run typecheck`.
{
  "extends": "./tsconfig.json",
  "include": ["internal/export/templates/app.js", "globals.d.ts"]
}
