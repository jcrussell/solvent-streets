#!/bin/sh
# states/clean/gate: runs after the agent edits, before the next routing.
# Exit non-zero to mark the iteration as gate-failed.
# Env: RALPH_REPO RALPH_ITER RALPH_STATE RALPH_PROMPT_FILE
#      RALPH_ITER_JSON
set -e
make build
make test
make lint
